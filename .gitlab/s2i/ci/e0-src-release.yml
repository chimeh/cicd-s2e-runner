stages:
  - src-release
src-mr-create:
  stage: src-release
  script:
     - gitlab-mr-create
  except:
    - master
    - /^hotfix.*$/
stages:
  - src-release
gitlab-auto-merge:
  stage: src-release
  script:
     - gitlab-auto-merge
  only:
    - /^hotfix.*$/